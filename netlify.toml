[build]
  publish = "static"

[functions]
  directory = "netlify/functions"

# Redirect /api/* calls to Netlify Functions
[[redirects]]
  from = "/api/read-mail"
  to = "/.netlify/functions/read-mail"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
